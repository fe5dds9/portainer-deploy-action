{
  "name": "portainer-deploy-action",
  "version": "1.0.0",
  "description": "GitHub Action to deploy Docker stacks to Portainer",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --license LICENSE",
    "test": "jest"
  },
  "keywords": [
    "github-actions",
    "portainer",
    "docker",
    "swarm",
    "deployment"
  ],
  "author": "autofleetcontrol",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "jest": "^29.0.0"
  }
}